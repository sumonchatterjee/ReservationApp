<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_root"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white"
    android:orientation="vertical"
    android:padding="@dimen/screen_padding">

    <TextView
        android:id="@+id/tv_plan_paragraph"
        style="@style/dotext.Medium"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@null"
        android:focusableInTouchMode="false"
        android:gravity="center"
        android:padding="@dimen/screen_padding_plus_half"
        android:text="@string/share_plan_paragraph" />

    <GridView
        android:id="@+id/gridview_share_apps"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:columnWidth="80dp"
        android:gravity="center"
        android:horizontalSpacing="@dimen/screen_padding"
        android:numColumns="2"
        android:stretchMode="columnWidth"
        android:verticalSpacing="@dimen/screen_padding" />

    <TextView
        android:id="@+id/tv_more_option"
        style="@style/dotext.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@null"
        android:clickable="true"
        android:focusableInTouchMode="false"
        android:padding="@dimen/screen_padding_plus_half"
        android:text="MORE OPTIONS"
        android:textColor="#B3B3B3"
        android:textSize="14sp"
        android:visibility="gone" />

</LinearLayout>